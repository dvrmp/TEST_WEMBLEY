import { ProvinceNameTransformerPipe } from './province-name-transformer.pipe';

describe('ProvinceNameTransformerPipe', () => {
  it('create an instance', () => {
    const pipe = new ProvinceNameTransformerPipe();
    expect(pipe).toBeTruthy();
  });
});
